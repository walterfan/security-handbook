# Compatibility wrapper for users who still run `make` from doc/.
REPO_ROOT := ..

.PHONY: help install html build clean serve publish pages-artifact

help install html build clean serve publish pages-artifact:
	@$(MAKE) -C "$(REPO_ROOT)" $@
